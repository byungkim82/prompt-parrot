# LLM 번역 히스토리 웹앱 - 프로젝트 요구사항 (최종)

## 프로젝트 개요

### 목적
- 여러 LLM에게 질문하기 전에 한국어를 영어로 번역
- **LLM 프롬프트에 최적화된 번역** 제공
- 번역된 프롬프트를 쉽게 복사하여 사용
- 모든 번역 히스토리를 클라우드에 안전하게 저장하고 관리

### 사용자
- **개인 전용 앱** (1인 사용자)
- 인증 필수 (타인 접근 불가)

### 사용 시나리오
1. 웹앱 접속 (로그인)
2. 한국어로 질문 작성
3. "번역" 버튼 클릭 → LLM 프롬프트 최적화된 영어 번역
4. 영어 번역 결과 확인 + 클립보드 복사
5. 필요시 번역 결과 수동 편집
6. 다른 LLM에 붙여넣기
7. 원본/편집본 모두 히스토리 자동 저장
8. 언제든지 히스토리 조회 및 재사용

---

## 핵심 기능

### MVP (v1.0) - Phase 1
- [ ] **인증 시스템** (개인 전용 접근 제어)
- [ ] 한국어 텍스트 입력 폼 (Gemini API 토큰 제한 내)
- [ ] Gemini 2.0 Flash API로 **LLM 프롬프트 최적화 영어 번역**
- [ ] 번역 결과 표시 및 클립보드 복사 버튼
- [ ] **번역 결과 수동 편집 기능**
- [ ] 번역 히스토리 저장 (Cloudflare D1 - **암호화**)
  - 원본 번역 + 편집 버전 모두 저장
- [ ] 저장된 히스토리 리스트 조회 (무한 스크롤, 20개씩)
- [ ] 에러 처리 (에러 메시지 + 재시도 버튼)
- [ ] 로딩 상태 표시 (스피너)
- [ ] **히스토리 CSV Export 기능**

### Phase 2 - 핵심 기능 개선
- [ ] LLM별 분류 (Claude, GPT-4, Gemini 등)
- [ ] 히스토리 검색 기능 (한국어/영어)
- [ ] UI/UX 개선

### Phase 3 - 고급 기능 (점진적)
- [ ] 주제별 태그 시스템
- [ ] 즐겨찾기/북마크
- [ ] 대화 쓰레드 관리
- [ ] 통계 대시보드 (사용량, 자주 쓰는 주제 등)
- [ ] Notion 동기화 옵션
- [ ] 다크모드
- [ ] **다른 번역 API 지원** (Claude, GPT 등)

---

## 기술 스택

### 인프라
- **호스팅:** Cloudflare Pages
- **데이터베이스:** Cloudflare D1 (SQLite) with **암호화**
- **서버리스 함수:** Cloudflare Workers
- **CDN:** Cloudflare (자동 포함)
- **도메인:** 커스텀 도메인 (Cloudflare 구매 및 연결)

### 프론트엔드
- **프레임워크:** Next.js (App Router) 또는 React + Vite
- **스타일링:** Tailwind CSS
- **상태관리:** React Query (데이터 페칭용)
- **UI 컴포넌트:** shadcn/ui (선택사항)

### API
- **번역 (MVP):** Gemini 2.0 Flash API
  - 무료 티어: 분당 15 requests, 일 1,500 requests
  - 유료 전환 허용: $0.075 / 1M 토큰
  - 프롬프트 최적화: "Translate this Korean text to English optimized for LLM prompts. Maintain technical accuracy and clarity."
- **향후 확장:** Claude API, GPT API 등

### 인증
- **방식:** Cloudflare Access, Google OAuth, 또는 단일 사용자 패스워드 인증
- **목적:** 개인 전용 접근 제어

### 보안
- **데이터 암호화:** D1 저장 데이터 암호화 (AES-256)
- **API 키 관리:** Cloudflare Workers 환경 변수

### 개발 도구
- TypeScript
- Git + GitHub
- ESLint + Prettier

---

## 데이터베이스 스키마

### translations 테이블
```sql
CREATE TABLE translations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  korean_text TEXT NOT NULL,
  english_text TEXT NOT NULL,          -- 원본 번역
  edited_english_text TEXT,             -- 수동 편집 버전 (nullable)
  is_edited BOOLEAN DEFAULT 0,          -- 편집 여부
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 편집 시간
  llm_used TEXT,                        -- Phase 2: 어떤 LLM에 사용했는지
  tags TEXT,                            -- Phase 3: 쉼표 구분 태그
  is_favorite BOOLEAN DEFAULT 0,        -- Phase 3: 즐겨찾기
  notes TEXT,                           -- Phase 3: 메모
  translation_api TEXT DEFAULT 'gemini' -- Phase 3: 사용한 번역 API
);

CREATE INDEX idx_created_at ON translations(created_at DESC);
CREATE INDEX idx_llm_used ON translations(llm_used);
CREATE INDEX idx_is_favorite ON translations(is_favorite);
```

### 암호화 전략
- **필드 레벨 암호화:** `korean_text`, `english_text`, `edited_english_text`
- **키 관리:** Cloudflare Workers Secrets
- **라이브러리:** Web Crypto API 또는 SubtleCrypto

---

## UI/UX 요구사항

### 레이아웃
- 심플하고 깔끔한 인터페이스
- 모바일 반응형
- 빠른 로딩 (< 1초)

### 주요 화면

#### 1. 로그인 화면
```
┌─────────────────────────────────────┐
│     LLM 프롬프트 번역 도구          │
├─────────────────────────────────────┤
│                                     │
│        [Google로 로그인]            │
│     또는 [패스워드로 로그인]         │
│                                     │
└─────────────────────────────────────┘
```

#### 2. 메인 화면 (번역)
```
┌─────────────────────────────────────┐
│  LLM 번역 도구          [히스토리]  │
├─────────────────────────────────────┤
│  [한국어 입력]                       │
│  ┌───────────────────────────────┐  │
│  │                               │  │
│  │  여러 줄 텍스트 입력 가능     │  │
│  │  (Gemini 토큰 제한 내)        │  │
│  └───────────────────────────────┘  │
│               [번역하기]             │
│         [⏳ 번역 중...]              │
│                                      │
│  [영어 결과] (LLM 프롬프트 최적화)  │
│  ┌───────────────────────────────┐  │
│  │  번역 결과 표시               │  │
│  │  [✏️ 편집 모드]               │  │
│  └───────────────────────────────┘  │
│        [📋 클립보드에 복사]         │
│                                      │
│  ❌ 에러: API 호출 실패             │
│        [🔄 재시도]                  │
└─────────────────────────────────────┘
```

#### 3. 히스토리 화면
```
┌─────────────────────────────────────┐
│  히스토리              [CSV Export]  │
├─────────────────────────────────────┤
│  [검색] (Phase 2)                   │
│  ┌───────────────────────────────┐  │
│  │ 🕐 2024-01-15 14:30           │  │
│  │ KR: 리액트 성능 최적화...     │  │
│  │ EN: React performance opt...  │  │
│  │ ✏️ 편집됨                     │  │
│  └───────────────────────────────┘  │
│  ┌───────────────────────────────┐  │
│  │ 🕐 2024-01-14 10:20           │  │
│  │ KR: 데이터베이스 스키마...    │  │
│  └───────────────────────────────┘  │
│  ...                                │
│  [🔄 더 불러오기] (무한 스크롤)    │
└─────────────────────────────────────┘
```

### 상호작용
- **클립보드 복사:** 버튼 클릭 시 복사 + "복사됨!" 피드백
- **편집 모드:** 번역 결과 직접 수정 가능, 저장 시 `edited_english_text`에 별도 저장
- **히스토리 클릭:** 상세 보기 모달 + 재사용 (클립보드 복사)
- **무한 스크롤:** 스크롤 하단 도달 시 자동 20개 추가 로드

---

## 비기능 요구사항

### 성능
- 번역 응답 시간: < 3초
- 페이지 로드: < 1초
- 히스토리 조회: < 500ms

### 보안
- **인증:** 개인 전용 접근 제어
- **데이터 암호화:** 저장 데이터 AES-256 암호화
- **API 키 보호:** Cloudflare Workers 환경 변수 (클라이언트 노출 방지)

### 안정성
- **에러 처리:** 사용자 친화적 메시지 + 재시도 옵션
- **데이터 보존:** 무제한 저장
- **백업:** 사용자 주도 CSV Export

### 확장성
- **API 전환 가능:** 모듈화된 번역 서비스 (Gemini → Claude/GPT 교체 용이)
- **점진적 기능 추가:** 모듈화된 코드 구조

### 비용 관리
- **Cloudflare 무료 티어 모니터링**
  - D1: 5GB 저장소, 일 50만 읽기
  - Pages: 500 빌드/월
- **유료 전환 허용:** 무료 티어 초과 시

---

## 개발 단계

### Phase 1: MVP (2-3일)
1. ✅ Next.js 프로젝트 초기화 + TypeScript
2. ✅ Cloudflare Pages 연동
3. ✅ 인증 시스템 구현 (Google OAuth 또는 단일 패스워드)
4. ✅ Gemini API 연동 (프롬프트 최적화 번역)
5. ✅ D1 데이터베이스 설정 + 암호화
6. ✅ 번역 기능 구현 (입력 → 번역 → 표시)
7. ✅ 편집 기능 구현
8. ✅ 클립보드 복사 버튼
9. ✅ 히스토리 CRUD (생성, 조회, 무한 스크롤)
10. ✅ CSV Export 기능
11. ✅ 에러 처리 + 로딩 상태
12. ✅ 기본 UI (Tailwind CSS)
13. ✅ 커스텀 도메인 연결
14. ✅ 배포

### Phase 2: 핵심 기능 개선 (1주)
15. LLM 분류 필드 추가 (사용한 LLM 태그)
16. 히스토리 검색 기능 (한국어/영어)
17. UI/UX 개선 및 반응형 최적화

### Phase 3: 고급 기능 (점진적)
18. 태그 시스템
19. 즐겨찾기
20. 통계 대시보드
21. Notion 연동
22. 다크모드
23. 다중 번역 API 지원 (Claude, GPT)

---

## API 명세

### Gemini 번역 API 호출 예시
```typescript
// Cloudflare Workers Function
export async function translateToEnglish(koreanText: string): Promise<string> {
  const apiKey = process.env.GEMINI_API_KEY;
  const prompt = `Translate the following Korean text to English, optimized for use as a prompt for Large Language Models (LLMs).
  Maintain technical accuracy, clarity, and natural phrasing suitable for AI interactions.

  Korean text:
  ${koreanText}

  English translation:`;

  const response = await fetch(`https://generativelanguage.googleapis.com/v1/models/gemini-2.0-flash:generateContent?key=${apiKey}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      contents: [{ parts: [{ text: prompt }] }]
    })
  });

  const data = await response.json();
  return data.candidates[0].content.parts[0].text;
}
```

### D1 데이터 암호화 예시
```typescript
// 암호화 헬퍼
async function encrypt(text: string, key: CryptoKey): Promise<string> {
  const encoder = new TextEncoder();
  const data = encoder.encode(text);
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const encryptedData = await crypto.subtle.encrypt(
    { name: 'AES-GCM', iv },
    key,
    data
  );
  return JSON.stringify({
    iv: Array.from(iv),
    data: Array.from(new Uint8Array(encryptedData))
  });
}

async function decrypt(encryptedText: string, key: CryptoKey): Promise<string> {
  const { iv, data } = JSON.parse(encryptedText);
  const decryptedData = await crypto.subtle.decrypt(
    { name: 'AES-GCM', iv: new Uint8Array(iv) },
    key,
    new Uint8Array(data)
  );
  const decoder = new TextDecoder();
  return decoder.decode(decryptedData);
}
```

---

## CSV Export 형식

```csv
ID,생성일,수정일,한국어,영어(원본),영어(편집),편집여부,사용 LLM,태그
1,2024-01-15 14:30,2024-01-15 14:35,"리액트 성능 최적화","React performance optimization","Optimize React app performance",TRUE,Claude,"React,Performance"
2,2024-01-14 10:20,2024-01-14 10:20,"데이터베이스 스키마","Database schema design",,FALSE,GPT-4,"Database"
```

---

## 보안 고려사항

### 인증 옵션 (택 1)
1. **Cloudflare Access:** Zero Trust 보안, IP 화이트리스트
2. **Google OAuth:** Google 계정 연동 (1인 이메일만 허용)
3. **단일 패스워드:** 환경 변수 저장 해시 비교 (간단)

### 데이터 보호
- **전송 중:** HTTPS (Cloudflare 자동)
- **저장 중:** AES-256-GCM 암호화
- **키 관리:** Cloudflare Workers Secrets (KMS)

---

## 참고 자료

- [Cloudflare Pages 문서](https://developers.cloudflare.com/pages/)
- [Cloudflare D1 문서](https://developers.cloudflare.com/d1/)
- [Cloudflare Workers 문서](https://developers.cloudflare.com/workers/)
- [Gemini API 문서](https://ai.google.dev/docs)
- [Next.js 문서](https://nextjs.org/docs)
- [Web Crypto API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API)

---

## 성공 기준

- ✅ 인증을 통해 본인만 접근 가능
- ✅ 번역 기능이 LLM 프롬프트 최적화되어 정확하고 빠르게 작동
- ✅ 번역 결과를 자유롭게 편집 가능
- ✅ 원본/편집본 모두 암호화되어 안전하게 저장
- ✅ 히스토리를 CSV로 Export 가능
- ✅ 인터페이스가 직관적이고 사용하기 편함
- ✅ 에러가 발생해도 사용자가 쉽게 재시도 가능
- ✅ 인프라가 안정적으로 장기 운영 가능
- ✅ 향후 다른 번역 API 추가 용이
- ✅ 필요한 기능을 점진적으로 추가 가능

---

## 다음 단계

1. **기술 스택 최종 선택**
   - Next.js vs React + Vite
   - 인증 방식 (Cloudflare Access vs OAuth vs 패스워드)

2. **개발 환경 설정**
   - 프로젝트 초기화
   - Cloudflare 계정 및 D1 생성
   - Gemini API 키 발급

3. **MVP 개발 시작**
