name = "prompt-parrot"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# D1 Database binding
# After creating the database with `npm run db:create`,
# uncomment and update the database_id below
# [[d1_databases]]
# binding = "DB"
# database_name = "prompt-parrot-db"
# database_id = "YOUR_DATABASE_ID_HERE"

[vars]
NODE_ENV = "production"

# Secrets (set via `wrangler secret put GEMINI_API_KEY`)
# GEMINI_API_KEY = "set via wrangler secret"
